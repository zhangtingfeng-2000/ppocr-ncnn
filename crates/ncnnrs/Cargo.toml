[package]
name = "ncnnrs"
version = "0.1.7"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
encoding_rs = "0.8"
libc = "0.2"

[build-dependencies]
bindgen = "0.72"

[lints.rust]
warnings = "allow" # deny, warn, allow, or forbid

[features]
cpu = [] # 默认启用vulkan，开启此feature将仅使用cpu
